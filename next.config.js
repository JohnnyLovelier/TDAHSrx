/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;
```

Commit.

---

**Fichier 3 : `src/app/globals.css`** (nouveau fichier)

Clique le bouton **"+"** → **"Create new file"**. Dans le champ "Name your file", tape exactement :
```
src/app/globals.css
